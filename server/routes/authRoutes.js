// Authentication routes
// /api/auth

const express = require('express');
const router = express.Router();

// TODO: Import controllers and middleware
// TODO: Define routes for register, login, and get current user

module.exports = router;
