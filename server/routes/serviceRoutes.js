// Service routes
// /api/services

const express = require('express');
const router = express.Router();

// TODO: Import controllers and middleware
// TODO: Define routes for service CRUD and reviews

module.exports = router;
